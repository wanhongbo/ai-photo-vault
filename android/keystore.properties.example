# 复制本文件为 keystore.properties 并填入真实值（keystore.properties 已加入 .gitignore，勿提交）。
#
# Google Play 应用签名（Play App Signing）说明：
# - 你本地用「上传密钥」对 AAB 签名后上传到 Play；用户设备安装的是由 Google 保管的「应用签名密钥」签名的版本。
# - 首次在 Play 管理中心创建应用并上传 AAB 时，按向导启用 Play 应用签名即可。
# - 务必备份上传密钥库与密码；丢失后可在 Play 控制台申请重置上传密钥，但流程较麻烦。
#
# 生成上传密钥库示例（在 android/ 目录下执行，按需改别名与有效期）：
#   keytool -genkeypair -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
#
storePassword=请填写密钥库密码
keyPassword=请填写密钥密码（可与 storePassword 相同）
keyAlias=upload
# 相对 android/ 根目录的路径，例如与 keystore.properties 同目录下放 upload-keystore.jks
storeFile=upload-keystore.jks
