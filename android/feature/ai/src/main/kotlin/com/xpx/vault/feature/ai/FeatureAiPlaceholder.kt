package com.xpx.vault.feature.ai

/**
 * AI Tab 特性模块占位。后续 PR2/PR3/PR4 将逐步迁入：
 *  - AiHomeViewModel（聚合 pending 计数 Flow）
 *  - AiSensitiveReviewScreen（待处理敏感列表 + 批量处置）
 *  - AiCleanupScreen（重复 / 模糊 / 废片分组）
 *  - AiPrivacyRedactScreen（脱敏预览 + 导出）
 *  - AiClassifyScreen（六分类 Virtual Album）
 *
 * 注意：AiHomeScreen UI 当前仍位于 :app，待 PR4 完成后整体迁移过来，避免
 * 一次性大改破坏现有导航链路。
 */
internal object FeatureAiPlaceholder
