---
description: 图片与图标素材默认采用高保真资源及 Android 放置约定
alwaysApply: true
---

# 图片素材（默认高保真）

本工程后续**凡涉及 UI 图片 / 图标 / 闪屏 / 插图**，默认按「高保真」处理，避免糊图与随意缩放低分辨率素材。

## 原则

1. **优先来源**：设计稿（Pixso / Figma 等）**按倍率导出**的 WebP，或经确认的 **矢量**（`vector` / SVG 转 Vector Drawable），禁止使用不明来源的小图硬拉大。
2. **位图**：至少覆盖常用密度；推荐由设计导出 **2x / 3x**（或提供 `xxhdpi` / `xxxhdpi` 成套切片）。单张精修主视觉可放在 `drawable-nodpi` 避免系统误缩放。
3. **矢量**：图标类优先 `vector`；路径需清晰，与设计主色一致，避免粗糙简化形。
4. **Compose**：`Image` / `painterResource` 引用上述资源；网络大图用 Coil 等并保持与设计分辨率策略一致。
5. **替代说明**：若暂时只有低清素材，须在 PR/提交说明中标注「待设计补高清」，并控制展示尺寸，避免全屏拉伸。

## 与「系统图标」的区别

业务品牌、闪屏、运营图等**必须用高保真设计导出**；系统控件可用的 Material 符号仅作缺省占位，不替代正式素材。
